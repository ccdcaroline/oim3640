**TYPE OUT = To get** 

cd folder = changes the directory (going into folder)

cd.. = Go up a level within folders 

dir = list files in a current folder 

mkdir folder = make a new folder 

pwd = print working directory (find where you are)

**To find where your file is located,** right click on the file in VS then press **"Reveal in File Explorer"**. 

When reading chapter notebook, you press **"Shift Enter"** so its esaier to read and to run the code. 

Can open repository in command prompt, by going on Github desktop, click top left repository, then open in command prompt. 

Then to open notebooks on browser, you type "cd notebooks" then enter. After you then type "jupyter lab" and it will open in browser. 

ctrl c = run in terminal, it closes what your running 

ctrl shift = command out, make a line into a comment # 

If you want to see the output on code in VS, like hello.py you have to print() to get an input. 

